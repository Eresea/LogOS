[package]
name = "logos-shell"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"

[lib]
test = true
bench = false

[dependencies]
logos-abi = { path = "../../abi" }
logos-ui = { path = "../../ui" }
logos-ui-compiler = { path = "../../ui-compiler" }
