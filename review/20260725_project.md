[Review made by nemotron-3-ultra-550b-a55b on 2026/07/25 13:35]

## Key

### 1. **Critical Infrastructure** (3 major items)
| Task | Why It Matters |
|------|----------------|
| **Capability-Backed Dynamic Allocator** | Replaces 15+ `ponytail` fixed arrays; enforces quotas at memory level; foundation for WASM runtime |
| **Service Manifest + Supervisor** | Declarative service management; enables boot profiles, service replacement, audit trails — core Platform v1 requirement |
| **Hardware Abstraction Layer (HAL)** | Extracts all inline asm/port I/O; enables host-side unit testing, ARM64/RISC-V ports, deterministic replay |

### 2. **High-Priority Architecture** (3 items)
- **Structured Error Types** — actionable diagnostics, capability-aware errors, automated recovery
- **Async/Await IPC + Lightweight Executor** — composable workflows, natural backpressure, WASM async foundation
- **G Terminal** — correct UTF-8/international text, selection integrity, Console v1 exit criteria

### 3. **Code Quality** (3 items)
- Consolidated test harness with structured output + host execution
- Custom Clippy lints for architectural rules (fixed arrays, inline asm, capability checks)
- Structured trace export (JSON/CBOR/Protobuf) for remote4. **Development Workflow** (5 items)
| Enhancement | Impact |
|-------------|--------|
| Host-side unit tests (no QEMU) | Sub-second feedback for 90% of changes |
| Pre-commit hooks (fmt, clippy, host tests, spellcheck) | Catches issues at commit time |
| GitHub Actions CI (host + QEMU) | Every PR verified; artifacts on failure |
| Architecture Decision Records (ADRs) | Discoverable, reviewable, superseded decisions |
| Dependency visualization script | CI gate for### 5. **Testing Strategy**
- Test pyramid: Unit → Component → E2E (QEMU)
- Property-based testing targets per module

### 6. **Documentation Gaps** (7 identified with priorities)

### 7. **Priority Matrix** — 10 tasks ranked by effort/impact/blocking with suggested 4-phase migration plan

---

The document is written as a living reference you can update as decisions are made. Want me to:
1. **Start implementing Phase 1** (allocator + HAL + host tests)?
2. **Create the ADR template** and first few records?
3. **Set up the GitHub Actions workflow** and pre-commit config?

I've created a comprehensive design document at `./DESIGN.md`. Here's a summary of what it covers:

## Key Sections

### 1. **Critical Infrastructure** (3 major items)
| Task | Why It Matters |
|------|----------------|
| **Capability-Backed Dynamic Allocator** | Replaces 15+ `ponytail` fixed arrays; enforces quotas at memory level; foundation for WASM runtime |
| **Service Manifest + Supervisor** | Declarative service management; enables boot profiles, service replacement, audit trails — core Platform v1 requirement |
| **Hardware Abstraction Layer (HAL)** | Extracts all inline asm/port I/O; enables host-side unit testing, ARM64/RISC-V ports, deterministic replay |

### 2. **High-Priority Architecture** (3 items)
- **Structured Error Types** — actionable diagnostics, capability-aware errors, automated recovery
- **Async/Await IPC + Lightweight Executor** — composable workflows, natural backpressure, WASM async foundation
- **Grapheme Cluster Terminal** — correct UTF-8/international text, selection integrity, Console v1 exit criteria

### 3. **Code Quality** (3 items)
- Consolidated test harness with structured output + host execution
- Custom Clippy lints for architectural rules (fixed arrays, inline asm, capability checks)
- Structured trace export (JSON/CBOR/Protobuf) for remote debugging

### 4. **Development Workflow** (5 items)
| Enhancement | Impact |
|-------------|--------|
| Host-side unit tests (no QEMU) | Sub-second feedback for 90% of changes |
| Pre-commit hooks (fmt, clippy, host tests, spellcheck) | Catches issues at commit time |
| GitHub Actions CI (host + QEMU) | Every PR verified; artifacts on failure |
| Architecture Decision Records (ADRs) | Discoverable, reviewable, superseded decisions |
| Dependency visualization script | CI gate for ring boundary violations |

### 5. **Testing Strategy**
- Test pyramid: Unit → Component → E2E (QEMU)
- Property-based testing targets per module

### 6. **Documentation Gaps** (7 identified with priorities)

### 7. **Priority Matrix** — 10 tasks ranked by effort/impact/blocking with suggested 4-phase migration plan

---

The document is written as a living reference you can update as decisions are made. Want me to:
1. **Start implementing Phase 1** (allocator + HAL + host tests)?
2. **Create the ADR template** and first few records?
3. **Set up the GitHub Actions workflow** and pre-commit config?