{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run LogOS UI extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
