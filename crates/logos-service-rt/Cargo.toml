[package]
name = "logos-service-rt"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
logos-abi = { path = "../logos-abi" }
logos-core = { path = "../logos-core" }
