[package]
name = "logos-terminal-service"
version = "0.1.0"
edition = "2024"

[dependencies]
logos-core = { path = "../logos-core" }
logos-terminal = { path = "../logos-terminal" }
uefi = { version = "0.35", features = ["panic_handler"] }
